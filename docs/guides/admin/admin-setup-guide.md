# Admin 권한 설정 가이드

AI Co-Learner 프로젝트의 관리자 권한 설정 및 관리 가이드

---

## 🎯 개요

이 가이드는 **관리자 권한을 부여하고 관리**하는 방법을 설명합니다.

### 권한 체계

| 역할 | 아이콘 | 권한 |
|------|--------|------|
| **ADMIN** | 👑 | 모든 관리 기능 (사용자 관리, 사용량 통계, 봇 템플릿) |
| **SUPER_USER** | ⭐ | 봇 템플릿 관리만 가능 |
| **USER** | 👤 | 일반 사용자 (학습 기능만) |

---

## 🚀 빠른 시작

### 1. 최초 관리자 설정

```bash
cd scripts

# 본인 계정을 ADMIN으로 설정
node manage-admin.mjs set your-username ADMIN
```

**출력 예시:**
```
✅ 역할 변경 완료!
   사용자: 홍길동 (@hong.gildong)
   UserId: 0a1b2c3d-4e5f-6789-abcd-ef0123456789
   이전 역할: USER
   새 역할: ADMIN
```

### 2. 역할 확인

```bash
# 특정 사용자 확인
node manage-admin.mjs get hong.gildong
```

**출력 예시:**
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
사용자 정보
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  이름: 홍길동
  아이디: @hong.gildong
  UserId: 0a1b2c3d-4e5f-6789-abcd-ef0123456789
  소속: ABC 회사
  레벨: Lv.5 (XP: 1250)
  역할: ADMIN
  가입일: 2025-01-08T05:30:00.000Z
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### 3. 관리자 목록 조회

```bash
# 모든 관리자 (ADMIN + SUPER_USER)
node manage-admin.mjs list
```

**출력 예시:**
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
관리자 목록 (총 5명)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. 👑 홍길동 (@hong.gildong)
   역할: ADMIN
   소속: ABC 회사
   가입일: 2025-01-08T05:30:00.000Z

2. 👑 김철수 (@kim.chulsoo)
   역할: ADMIN
   소속: XYZ 대학교
   가입일: 2025-01-07T12:00:00.000Z

3. ⭐ 이영희 (@lee.younghee)
   역할: SUPER_USER
   소속: ABC 회사
   가입일: 2025-01-06T09:15:00.000Z
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

## 📖 상세 사용법

### 역할 변경

```bash
# ADMIN으로 승격
node manage-admin.mjs set username ADMIN

# SUPER_USER로 변경
node manage-admin.mjs set username SUPER_USER

# 일반 사용자로 강등
node manage-admin.mjs set username USER
```

### 전체 사용자 목록

```bash
# 전체 사용자 + 역할별 통계
node manage-admin.mjs list-all
```

**출력 예시:**
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
전체 사용자 목록 (총 142명)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📊 역할별 통계:
   ADMIN: 3명 👑
   SUPER_USER: 2명 ⭐
   USER: 137명 👤

1. 👑 홍길동 (@hong.gildong)
   역할: ADMIN | 레벨: Lv.5 | 소속: ABC 회사

2. 👑 김철수 (@kim.chulsoo)
   역할: ADMIN | 레벨: Lv.3 | 소속: XYZ 대학교
...
```

### 도움말 보기

```bash
node manage-admin.mjs --help
```

---

## 🛠️ 실제 사용 시나리오

### 시나리오 1: 신규 관리자 임명

```bash
# 1. 사용자 정보 확인
node manage-admin.mjs get new.manager

# 2. SUPER_USER로 임명 (템플릿 관리 권한)
node manage-admin.mjs set new.manager SUPER_USER

# 3. 확인
node manage-admin.mjs list
```

### 시나리오 2: 최고 관리자 임명

```bash
# 1. ADMIN 권한 부여
node manage-admin.mjs set ceo.name ADMIN

# 2. 현재 ADMIN 목록 확인
node manage-admin.mjs list

# 3. 전체 관리자 통계 확인
node manage-admin.mjs list-all
```

### 시나리오 3: 권한 회수

```bash
# 1. 현재 역할 확인
node manage-admin.mjs get old.admin

# 2. 일반 사용자로 강등
node manage-admin.mjs set old.admin USER

# 3. 확인
node manage-admin.mjs get old.admin
```

### 시나리오 4: 학교 선생님 권한 부여

```bash
# 여러 선생님을 SUPER_USER로 설정
node manage-admin.mjs set teacher1 SUPER_USER
node manage-admin.mjs set teacher2 SUPER_USER
node manage-admin.mjs set teacher3 SUPER_USER

# 확인
node manage-admin.mjs list
```

---

## 🔍 권한별 기능 상세

### ADMIN (👑 최고 관리자)

**접근 가능한 기능:**
1. **사용자 관리**
   - 전체 사용자 목록 조회
   - 사용자 정보 수정 (이름, 소속, 비밀번호)
   - 역할 변경 (USER ↔ SUPER_USER ↔ ADMIN)
   - 사용자 차단/해제

2. **사용량 & 비용 관리**
   - 전체 사용량 통계 (메시지, 토큰, 비용)
   - 사용자별 사용량 분석
   - 일별 비용 차트
   - 월간 예상 비용 계산

3. **봇 템플릿 관리**
   - 템플릿 생성/수정/삭제
   - 역량 매핑 설정
   - 시스템 프롬프트 편집

**AdminPanel UI 접근:**
- Templates 탭 ✅
- Users 탭 ✅
- Usage & Cost 탭 ✅

---

### SUPER_USER (⭐ 부관리자)

**접근 가능한 기능:**
1. **봇 템플릿 관리**
   - 템플릿 생성/수정/삭제
   - 역량 매핑 설정
   - 시스템 프롬프트 편집

**제한 사항:**
- ❌ 사용자 관리 불가
- ❌ 사용량 통계 조회 불가
- ❌ 역할 변경 불가

**AdminPanel UI 접근:**
- Templates 탭 ✅
- Users 탭 ❌
- Usage & Cost 탭 ❌

---

### USER (👤 일반 사용자)

**접근 가능한 기능:**
- Dashboard (역량 차트, 뱃지, 추천 봇)
- ChatRoom (AI 채팅)
- Daily Quests (일일 퀘스트)
- KnowledgeBase (학습 자료)
- User Profile (개인 설정)

**제한 사항:**
- ❌ Admin Panel 접근 불가
- ❌ 다른 사용자 정보 조회 불가
- ❌ 시스템 설정 변경 불가

---

## 🔐 보안 고려사항

### 1. ADMIN 권한 최소화
- ADMIN은 **2-3명으로 제한** 권장
- 조직의 최고 책임자에게만 부여
- 정기적으로 ADMIN 목록 검토

### 2. SUPER_USER 활용
- 일반 관리 업무는 SUPER_USER로 충분
- 선생님, 팀 리더 등에게 적합
- 템플릿 관리만 필요한 경우 사용

### 3. 권한 변경 로그
- DynamoDB `ai-co-learner-users` 테이블에 `updatedAt` 기록
- CloudWatch Logs에서 변경 히스토리 확인 가능

### 4. 정기 감사
```bash
# 월 1회 관리자 목록 검토
node manage-admin.mjs list

# 분기 1회 전체 사용자 역할 검토
node manage-admin.mjs list-all
```

---

## ⚠️ 주의사항

### 역할 변경 시 주의
1. **자기 자신의 ADMIN 권한 제거 금지**
   ```bash
   # 🚫 위험: 마지막 ADMIN이 본인을 강등하면 복구 불가
   node manage-admin.mjs set myself USER
   ```

2. **최소 1명의 ADMIN 유지**
   - 항상 2-3명의 ADMIN 유지 권장
   - 1명만 남은 경우 변경 전 백업 ADMIN 임명

3. **프로덕션 환경에서 신중히 실행**
   - 변경 전 반드시 확인: `node manage-admin.mjs get username`
   - Typo 주의 (username 정확히 입력)

### AWS 자격 증명 필요
- 스크립트는 DynamoDB에 직접 접근
- AWS CLI 설정 필요 (`aws configure`)
- IAM 권한: `dynamodb:Scan`, `dynamodb:UpdateItem`

---

## 🔧 트러블슈팅

### "User not found" 에러

**원인**: username이 정확하지 않거나 존재하지 않는 사용자

**해결:**
```bash
# 전체 사용자 목록에서 정확한 username 확인
node manage-admin.mjs list-all

# username은 대소문자 구분
# @hong.gildong (O)
# @Hong.Gildong (X)
```

### AWS 자격 증명 에러

**원인**: AWS 자격 증명 미설정

**해결:**
```bash
# AWS CLI 설정
aws configure
# AWS Access Key ID: YOUR_KEY
# AWS Secret Access Key: YOUR_SECRET
# Default region name: ap-northeast-2
# Default output format: json

# 테스트
aws sts get-caller-identity
```

### DynamoDB 접근 에러

**원인**: IAM 권한 부족

**해결:**
```bash
# 테이블 접근 확인
aws dynamodb describe-table \
  --table-name ai-co-learner-users \
  --region ap-northeast-2

# 필요 IAM 권한:
# - dynamodb:Scan
# - dynamodb:GetItem
# - dynamodb:UpdateItem
```

### 역할이 변경되지 않음

**원인**: 브라우저 캐시 또는 세션 만료

**해결:**
```bash
# 1. DynamoDB에서 직접 확인
node manage-admin.mjs get username

# 2. 브라우저에서 로그아웃 후 재로그인
# 3. 브라우저 캐시 삭제 (Ctrl+Shift+Delete)
```

---

## 📚 관련 문서

- [scripts/README.md](../scripts/README.md) - 스크립트 전체 목록
- [docs/aws-architecture.md](./aws-architecture.md) - AWS 아키텍처
- [src/pages/AdminPanel.tsx](../src/pages/AdminPanel.tsx) - Admin UI 구현

---

## 🎓 FAQ

### Q1. username을 어떻게 확인하나요?
```bash
# 방법 1: 전체 사용자 목록 조회
node manage-admin.mjs list-all

# 방법 2: Admin Panel UI에서 확인
# Users 탭 → @username 확인
```

### Q2. SUPER_USER와 ADMIN의 차이는?
- **SUPER_USER**: 봇 템플릿 관리만 가능
- **ADMIN**: 모든 관리 기능 (사용자, 사용량, 템플릿)

### Q3. 역할 변경이 즉시 반영되나요?
- **DynamoDB**: 즉시 반영 ✅
- **UI**: 로그아웃 후 재로그인 필요 (세션 갱신)

### Q4. 학교에서 여러 선생님에게 권한을 주려면?
```bash
# 선생님들은 SUPER_USER로 설정 (템플릿 관리)
node manage-admin.mjs set teacher1 SUPER_USER
node manage-admin.mjs set teacher2 SUPER_USER

# 교장/교감은 ADMIN (전체 관리)
node manage-admin.mjs set principal ADMIN
```

### Q5. 실수로 잘못된 역할을 부여했어요!
```bash
# 즉시 원래 역할로 복구
node manage-admin.mjs set username USER

# 확인
node manage-admin.mjs get username
```

---

**마지막 업데이트**: 2025-01-08
