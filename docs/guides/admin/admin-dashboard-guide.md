# 관리자 대시보드 가이드

AI Co-Learner 관리자 대시보드 사용 가이드

---

## 🎯 개요

관리자 대시보드는 플랫폼의 **실시간 상태를 한눈에 파악**할 수 있는 모니터링 도구입니다.
데이터 기반 의사결정과 문제의 조기 발견을 지원합니다.

### 주요 기능
- ✅ **실시간 메트릭**: 5초 안에 서비스 상태 파악
- 📊 **핵심 지표**: 활성 사용자, 대화 수, 역량 평균, 퀘스트 완료율
- ⚠️ **이탈 위험 감지**: 7일 미접속 사용자 자동 표시
- 🤖 **인기 봇 분석**: Top 3 봇 사용량 추적
- 📈 **활동 패턴**: 시간대별 사용자 활동 그래프

---

## 📊 대시보드 구성

### 1. 메트릭 카드 (5개)

#### 📘 오늘 활성 사용자
```
표시: X명
부제: 평균 Y건/사용자
색상: 파란색
```
- **의미**: 오늘 메시지를 보낸 고유 사용자 수
- **계산**: chat-sessions 테이블에서 오늘(00:00~) 메시지 필터링
- **활용**: 일일 참여도 측정, 요일별 패턴 파악

#### 📗 오늘 대화 수
```
표시: X건
부제: Y명이 활동 중
색상: 초록색
```
- **의미**: 오늘 전송된 총 메시지 수
- **계산**: chat-sessions 테이블 레코드 카운트
- **활용**: 플랫폼 활성도 측정, 피크 시간 대비

#### 📕 전체 평균 역량
```
표시: X점
부제: 총 Y명 사용자
색상: 보라색
```
- **의미**: 전체 사용자의 6개 역량 평균 점수
- **계산**: user-competencies 테이블에서 모든 역량 점수 평균
- **활용**: 서비스 효과성 측정, 역량 트렌드 분석

#### 📙 퀘스트 완료율
```
표시: X%
부제: Y/Z개 완료
색상: 청록색
```
- **의미**: 오늘 할당된 퀘스트 중 완료된 비율
- **계산**: daily-quests 테이블에서 오늘 날짜 퀘스트 필터링
- **활용**: 사용자 동기부여 측정, 퀘스트 난이도 조정

#### 🔴 7일 미접속
```
표시: X명
부제: 이탈 위험 사용자
색상: 빨간색
경고: 10명 초과 시 깜빡이는 점 표시
```
- **의미**: 7일 이상 로그인하지 않은 사용자 수
- **계산**: users 테이블 lastLoginAt 필드 확인
- **활용**: 이탈 위험 조기 감지, 리텐션 전략 수립

---

### 2. 인기 봇 Top 3

```
┌──────────────────────────────────┐
│ 인기 봇 Top 3                     │
├──────────────────────────────────┤
│ 1️⃣ 논리적 사고 코치       87회   │
│ 2️⃣ 소크라테스 질문법      65회   │
│ 3️⃣ 창의력 마스터          52회   │
└──────────────────────────────────┘
```

- **의미**: 가장 많이 생성된 봇 템플릿 순위
- **계산**: user-bots 테이블에서 templateId별 카운트
- **활용**:
  - 인기 봇 우선 개선
  - 저조한 봇 템플릿 개선 필요성 파악
  - 신규 봇 추가 시 참고

---

### 3. 시간대별 활동 차트

```
📈 시간대별 활동 (오늘)
- 파란선: 활성 사용자 수
- 초록선: 메시지 수
- X축: 00:00 ~ 23:00 (24시간)
- Y축: 카운트
```

- **의미**: 24시간 동안 시간대별 활동 패턴
- **계산**: chat-sessions 테이블에서 timestamp를 시간대별로 집계
- **활용**:
  - 피크 타임 파악 (예: 오전 9시, 오후 9시)
  - 리소스 최적화 (Lambda, DynamoDB)
  - 마케팅 시간 결정 (알림, 이메일 발송)

---

## 🚀 사용 방법

### 접근하기

1. **Admin 권한 필요**
   - Role: `ADMIN` (SUPER_USER는 접근 불가)
   - 권한 부여: `scripts/manage-admin.mjs` 사용

2. **대시보드 열기**
   ```
   Admin Panel → 대시보드 탭 클릭
   ```
   - 기본 뷰로 자동 표시 (view='dashboard')

### 데이터 새로고침

```
우측 상단 "새로고침" 버튼 클릭
```
- API 호출: `GET /admin/dashboard?adminUserId={id}`
- 응답 시간: 평균 2-3초 (DynamoDB Scan 포함)

---

## 📖 활용 시나리오

### 시나리오 1: 일일 모니터링
**시간**: 매일 오전 9시

```
1. 대시보드 열기
2. "오늘 활성 사용자" 확인
   - 전날 대비 증감 파악
3. "7일 미접속" 확인
   - 10명 이상이면 리텐션 전략 실행
4. "퀘스트 완료율" 확인
   - 30% 이하면 난이도 조정 검토
```

---

### 시나리오 2: 주간 분석
**시간**: 매주 월요일

```
1. "인기 봇 Top 3" 확인
   - 지난 주 대비 순위 변화 파악
   - 하위권 봇 개선 계획 수립
2. "시간대별 활동" 확인
   - 피크 타임 패턴 분석
   - 공지사항 발송 시간 최적화
3. "전체 평균 역량" 확인
   - 주간 성장률 계산
```

---

### 시나리오 3: 문제 대응
**상황**: 사용자 이탈 급증

```
1. "7일 미접속" 확인
   - 이탈 사용자 목록 파악
2. "Users 탭" → 이탈 사용자 필터링
   - 마지막 로그인 날짜 확인
   - 이메일 리마인더 발송
3. "오늘 활성 사용자" 트렌드 확인
   - 최근 7일 데이터와 비교
   - 원인 분석 (신규 기능, 버그 등)
```

---

## 🔧 기술 상세

### API 엔드포인트
```
GET /admin/dashboard?adminUserId={userId}
```

### 응답 구조
```typescript
{
  today: {
    activeUsers: number;          // 오늘 활성 사용자
    totalMessages: number;        // 오늘 총 메시지
    avgMessagesPerUser: number;   // 평균 메시지/사용자
    questCompletionRate: number;  // 퀘스트 완료율 (%)
    completedQuests: number;      // 완료된 퀘스트 수
    totalQuests: number;          // 총 퀘스트 수
  };
  overall: {
    totalUsers: number;           // 전체 사용자 수
    avgCompetencyScore: number;   // 평균 역량 점수
    inactiveUsers7d: number;      // 7일 미접속 사용자
  };
  topBots: [
    {
      botId: string;
      name: string;
      usageCount: number;
    }
  ];
  hourlyActivity: [
    {
      hour: string;               // "00:00" ~ "23:00"
      activeUsers: number;
      messages: number;
    }
  ];
  timestamp: string;              // ISO 8601
}
```

### DynamoDB 쿼리

1. **오늘 활성 사용자**
   ```
   Table: chat-sessions
   Filter: timestamp >= todayStart
   Aggregation: Unique userId count
   ```

2. **역량 평균**
   ```
   Table: user-competencies
   Scan: All records
   Calculation: Average of 6 competency scores
   ```

3. **인기 봇**
   ```
   Table: user-bots
   Scan: All records
   Aggregation: Count by templateId
   Join: bot-templates for names
   ```

4. **퀘스트 완료율**
   ```
   Table: daily-quests
   Filter: questDate = today
   Calculation: completedQuests / totalQuests * 100
   ```

5. **이탈 위험**
   ```
   Table: users
   Scan: All records
   Filter: lastLoginAt < (now - 7 days)
   ```

---

## ⚠️ 주의사항

### 성능
- **Scan 사용**: users, user-bots, competencies 테이블은 전체 스캔
- **응답 시간**: 사용자 수에 비례 증가 (100명: ~2초, 1000명: ~5초)
- **최적화 필요 시점**: 1000명 이상 (GSI 또는 집계 테이블 필요)

### 비용
- **DynamoDB Read Units**: 대시보드 1회 조회 시 ~50 RCU 소비
- **On-Demand 요금**: $0.00025/RCU → 약 $0.0125/조회
- **월간 비용 추정**: 하루 10회 조회 × 30일 = $3.75

### 캐싱
- **현재**: 캐싱 없음 (항상 실시간 데이터)
- **권장**: 5분 캐싱 (Lambda 메모리 또는 ElastiCache)
- **효과**: 비용 80% 절감, 응답 속도 2배 향상

---

## 🔄 향후 개선 계획

### Phase 1: 데이터 캐싱
- Lambda 메모리에 5분 캐싱
- 응답 시간: 2초 → 0.5초
- 비용: 80% 절감

### Phase 2: 고급 필터링
- 날짜 범위 선택 (7일, 30일, 90일)
- 조직별 필터링
- 역할별 필터링

### Phase 3: 알림 시스템
- 이탈 위험 10명 초과 시 이메일 발송
- 퀘스트 완료율 30% 미만 시 알림
- 일일 리포트 자동 생성

### Phase 4: 비교 분석
- 전일/전주 대비 증감률 표시
- 월간 트렌드 차트
- 목표 설정 및 달성률

---

## 📚 관련 문서

- [관리자 권한 설정](./admin-setup-guide.md) - ADMIN 역할 부여 방법
- [사용량 추적 가이드](./usage-tracking-guide.md) - 비용 모니터링
- [AWS 아키텍처](./aws-architecture.md) - DynamoDB 테이블 구조

---

## 🎓 FAQ

### Q1. 대시보드가 느려요 (5초 이상)
**원인**: 사용자 수 증가로 인한 Scan 시간 증가

**해결**:
1. Lambda 타임아웃 확인 (60초 설정 권장)
2. DynamoDB On-Demand 용량 확인
3. 캐싱 도입 검토

### Q2. "7일 미접속"이 정확하지 않아요
**원인**: lastLoginAt 업데이트 로직 누락

**확인**:
```javascript
// Layout.tsx에서 로그인 시 lastLoginAt 업데이트 확인
await UserService.updateUserProfile(userId, name, organization);
```

### Q3. 인기 봇이 표시되지 않아요
**원인**: 사용자가 아직 봇을 생성하지 않음

**해결**:
- 최소 1개 이상의 봇이 생성되어야 표시됨
- 테스트 환경에서 수동으로 봇 생성 권장

### Q4. 시간대별 차트가 비어있어요
**원인**: 오늘 메시지가 없거나 timestamp 오류

**확인**:
```bash
# DynamoDB에서 오늘 메시지 확인
aws dynamodb scan --table-name ai-co-learner-chat-sessions \
  --filter-expression "begins_with(#date, :today)" \
  --expression-attribute-names '{"#date":"timestamp"}' \
  --expression-attribute-values '{":today":{"N":"'$(date +%s000)'"}}' \
  --max-items 5
```

### Q5. SUPER_USER가 대시보드를 못 봐요
**의도된 동작**: 대시보드는 ADMIN 전용

**변경 방법**:
```typescript
// src/pages/AdminPanel.tsx
if (view === 'dashboard' && (currentUser.role === Role.ADMIN || currentUser.role === Role.SUPER_USER)) {
  loadDashboardStats();
}
```

---

**마지막 업데이트**: 2026-01-12
**작성자**: Claude Sonnet 4.5
